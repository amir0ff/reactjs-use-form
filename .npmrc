# Use hoisted node_modules for compatibility with existing tools
node-linker=hoisted

# Enable pre/post scripts (will be default in pnpm v9)
enable-pre-post-scripts=true
 
# Prevent pnpm from creating a shamefully-hoist for better dependency management
shamefully-hoist=false 